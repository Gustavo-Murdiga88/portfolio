import { ScrollerContainer } from "./scroller-container";
import { ScrollerItem } from "./scroller-item";
import { ScrollerSection } from "./scroller-section";

export const Scroller = {
	Container: ScrollerContainer,
	Item: ScrollerItem,
	Section: ScrollerSection,
};
